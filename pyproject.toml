[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "corporateEthicsData"
version = "1.0.0"
dependencies = [
    "openrouter",
    "python-dotenv"
]

[tool.setuptools.package-data]
corporateEthicsData = ["*.json"]